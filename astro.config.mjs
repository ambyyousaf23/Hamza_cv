import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://ambyyousaf23.github.io/Hamza_cv', // Required for relative asset paths
  base: '/Hamza_cv/', // <--- This is important!
});
